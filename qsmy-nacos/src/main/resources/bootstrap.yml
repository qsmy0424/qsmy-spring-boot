server:
  port: 18084
spring:
  application:
    name: service-nacos
  cloud:
    nacos:
      server-addr: ${host}
      discovery:
        server-addr: ${host}
        namespace: ${namespace}
        group: test
      config:
        group: test
        file-extension: yaml
        namespace: ${namespace}
        extension-configs:
                 - data-id: test.yml
                   group: test
